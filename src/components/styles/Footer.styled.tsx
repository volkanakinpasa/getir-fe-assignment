import tw, { styled } from 'twin.macro';

const Footer = styled.footer`
  margin-top: 136px;
  ${tw`w-full flex items-center justify-center text-center py-6 text-primaryBlue text-xs`}
`;

export default Footer;
